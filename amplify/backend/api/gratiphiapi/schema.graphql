type Sponsor @model {
  id: ID!
  name: String!
  logo: String
  users: [User] @connection(keyName: "bySponsorID", fields: ["id"])
  donee: [Donee] @conenction(keyName: "bySponsorID", fields: ["id"])
}

type User @model @key(name: "bySponsorID", fields: ["sponsorID"]) {
  id: ID!
  name: String!
  lastName: String!
  email: String!
  sponsorID: ID!
  sponsor: Sponsor @connection(fields: ["sponsorID"])
}

type Donee @model {
  id: ID!
  first_name: String!
  last_name: String!
  location: String!
  smallBiography: String!
  fullBiography: String!
  sponsor: Sponsor @connection(fields: ["sponsorID"])
  donations: Donations @connection(fields: ["donationsID"])
}

type Donations @model {
  id: ID!
  user: User @connection(fields: ["userID"])
  donee: Donee @connection(fields: ["doneeID"])
}
